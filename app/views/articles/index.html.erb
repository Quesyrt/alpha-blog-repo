<h1>Liste des articles ALPHA !</h1>

<%= link_to 'Home', root_path %>
<p><%= link_to "Créer un nouvel article", new_article_path %> </p>

<table>
    <tr>
        <th>Titre</th>
        <th>Descriptions</th>
    </tr>
    
    <% @articles.each do |article| %>
        <tr>
            <td><%= article.title %></td>
            <td><%= article.description %></td>
            <td><%= link_to "Editer", edit_article_path(article) %></td>
            <td><%= link_to "Afficher", article_path(article) %></td>
            <td><%= link_to "Supprimer", article_path(article), method: :delete %></td>
            
        </tr>
    <% end %>
    
</table>